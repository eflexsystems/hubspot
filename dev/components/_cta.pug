// cta
mixin cta(args)
    | {% cta "#{args.name}" label="#{args.name}" guid="#{args.guid ? args.guid : 'f45a304e-b763-4049-bff9-b08abe1db12a'}" extra_classes="#{args.variant ? args.variant : null} text-uppercase #{args.class ? args.class : null}" %}