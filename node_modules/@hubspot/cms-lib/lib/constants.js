const ALLOWED_EXTENSIONS = new Set(['css', 'js', 'json', 'html']);

/**
 * `draft` for buffer APIs.
 *
 * @enum {string}
 */
const Mode = {
  draft: 'draft',
  publish: 'publish',
};

const DEFAULT_MODE = Mode.publish;

module.exports = {
  Mode,
  ALLOWED_EXTENSIONS,
  DEFAULT_MODE,
};
